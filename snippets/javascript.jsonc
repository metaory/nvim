{
  "rdc": {
    "prefix": "rdc",
    "body": "reduce((${1:acc}, ${2:cur}) => {\n\t${0}\n\treturn ${1};\n}${4:, initial})"
  },
  "ent": {
    "prefix": "ent",
    "body": ["entries()$1"],
      "description": "The Object.entries() method returns an array of a given object's own enumerable string-keyed property key-value pairs."
  }
}
